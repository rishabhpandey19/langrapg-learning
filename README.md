# langraph-youtube

A simple starter Python project created in the workspace.

## Structure

- `src/langraph_youtube/` - package source code
- `tests/` - test suite

## Run

```bash
python -m pip install -e .[dev]
python -m langraph_youtube.cli
```
